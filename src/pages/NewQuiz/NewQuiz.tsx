export function NewQuiz ()  {
    return ( 
        <div>
            <h1>New Quiz</h1>
            {/* there should be form with title and description of quiz  */}
            {/* each question should include type of question, question input,
            answers input where user can check one (or more if it is a multivariant question) right questions */}
            {/* use react hook form */}
        </div>
    );
}
